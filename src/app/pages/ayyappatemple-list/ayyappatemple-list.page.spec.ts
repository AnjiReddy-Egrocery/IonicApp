import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AyyappatempleListPage } from './ayyappatemple-list.page';

describe('AyyappatempleListPage', () => {
  let component: AyyappatempleListPage;
  let fixture: ComponentFixture<AyyappatempleListPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AyyappatempleListPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
