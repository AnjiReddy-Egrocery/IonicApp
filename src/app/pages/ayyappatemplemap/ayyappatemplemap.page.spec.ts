import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AyyappatemplemapPage } from './ayyappatemplemap.page';

describe('AyyappatemplemapPage', () => {
  let component: AyyappatemplemapPage;
  let fixture: ComponentFixture<AyyappatemplemapPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AyyappatemplemapPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
