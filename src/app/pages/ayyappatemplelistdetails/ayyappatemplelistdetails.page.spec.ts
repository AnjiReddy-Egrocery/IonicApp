import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AyyappatemplelistdetailsPage } from './ayyappatemplelistdetails.page';

describe('AyyappatemplelistdetailsPage', () => {
  let component: AyyappatemplelistdetailsPage;
  let fixture: ComponentFixture<AyyappatemplelistdetailsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AyyappatemplelistdetailsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
