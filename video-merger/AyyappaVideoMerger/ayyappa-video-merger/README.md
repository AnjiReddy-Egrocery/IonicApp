# ayyappavideomerger

Native Video Merger Plugin

## Install

To use npm

```bash
npm install ayyappavideomerger
````

To use yarn

```bash
yarn add ayyappavideomerger
```

Sync native files

```bash
npx cap sync
```

## API

<docgen-index>

* [`merge(...)`](#merge)

</docgen-index>

<docgen-api>
<!--Update the source file JSDoc comments and rerun docgen to update the docs below-->

### merge(...)

```typescript
merge(options: { video: string; poster: string; }) => Promise<{ path: string; }>
```

| Param         | Type                                            |
| ------------- | ----------------------------------------------- |
| **`options`** | <code>{ video: string; poster: string; }</code> |

**Returns:** <code>Promise&lt;{ path: string; }&gt;</code>

--------------------

</docgen-api>
